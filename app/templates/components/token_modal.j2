{# Token management modal #}
<div id="tokens-modal" class="modal" style="display: none;">
    <div class="modal-content">
        <div class="modal-header">
            <h2>API Tokens</h2>
            <button id="close-tokens-modal" class="close-btn">&times;</button>
        </div>
        <div class="modal-body">
            <div class="tokens-section">
                <p class="tokens-description">
                    API tokens allow you to authenticate with the QueryWeaver API without using OAuth. 
                    Keep your tokens secure and don't share them publicly.
                </p>
                
                <div class="tokens-actions">
                    <button id="generate-token-btn" class="btn">Generate New Token</button>
                </div>

                {# Token generation success message #}
                <div id="token-generated" class="token-generation-result" style="display: none;">
                    <div class="alert alert-success">
                        <h4>Token Generated Successfully!</h4>
                        <p><strong>Important:</strong> This is the only time you'll see this token. Copy it now and store it securely.</p>
                        <div class="token-display">
                            <div class="token-input-wrap">
                                <input type="password" id="new-token-value" readonly class="token-input" aria-label="API token">
                                <button id="toggle-token-visibility" class="btn btn-secondary" aria-label="Show token">Show</button>
                            </div>
                            <button id="copy-token-btn" class="btn btn-secondary">Copy</button>
                        </div>
                    </div>
                </div>

                {# Token list #}
                <div class="tokens-list">
                    <h3>Your Tokens</h3>
                    <div id="tokens-container">
                        <p id="no-tokens-message" class="no-tokens">You don't have any API tokens yet.</p>
                        <div id="tokens-table" style="display: none;">
                            <table class="tokens-table">
                                <thead>
                                    <tr>
                                        <th>Token</th>
                                        <th>Created</th>
                                        <th>Actions</th>
                                    </tr>
                                </thead>
                                <tbody id="tokens-tbody">
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

{# Token deletion confirmation modal #}
<div id="delete-token-modal" class="modal" style="display: none;">
    <div class="modal-content">
        <div class="modal-header">
            <h2>Delete Token</h2>
            <button id="close-delete-token-modal" class="close-btn">&times;</button>
        </div>
        <div class="modal-body">
            <p>Are you sure you want to delete this token? This action cannot be undone.</p>
            <p><strong>Token ending in:</strong> <span id="delete-token-last4"></span></p>
            <div class="modal-actions">
                <button id="confirm-delete-token" class="btn btn-danger">Delete Token</button>
                <button id="cancel-delete-token" class="btn btn-secondary">Cancel</button>
            </div>
        </div>
    </div>
</div>