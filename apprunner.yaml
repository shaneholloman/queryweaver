version: 1.0
runtime: python3.12
build:
  commands:
    build:
      - pip install pipenv
      - pipenv install --deploy --ignore-pipfile
run:
  command: pipenv run uvicorn main:app --host 0.0.0.0 --port 8000
  network:
    port: 8000